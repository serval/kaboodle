layout {
    pane split_direction="vertical" {
        pane {
            command "cargo"
            args "run" "--" "--identity" "left"
        }
        pane {
            command "cargo"
            args "run" "--" "--identity" "right"
        }
    }
}